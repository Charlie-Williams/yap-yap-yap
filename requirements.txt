# Audio capture is platform-specific:
#   Windows - WASAPI loopback via PyAudioWPatch
#   macOS   - mic + virtual-loopback (BlackHole) device via sounddevice
PyAudioWPatch>=0.2.12.7; sys_platform == "win32"
sounddevice>=0.4.6; sys_platform == "darwin"
numpy>=1.24
scipy>=1.10
faster-whisper>=1.0.0
huggingface_hub>=0.20
pystray>=0.19
Pillow>=10.0
